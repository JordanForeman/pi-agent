---
description: Design an isolated, blinded evaluation of a skill or prompt
---
In the parent session, load and follow `skill-evaluation` plus `evidence-report` for: $@

Keep evaluation orchestration in the parent. Do not delegate its design or coordination to `code-explorer` or any ordinary child. Run the guide's isolated candidate/judge workflow and report the resulting evidence.
